name: travelbuddy_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  intl: ^0.20.2
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  geocoding: ^4.0.0
  webview_flutter: ^4.2.2
  flutter_map: ^8.1.1

  latlong2: ^0.9.0
  http: ^1.1.0
  provider: ^6.1.1
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.6
  freezed: ^2.5.0
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

  assets:
    - assets/icon/IconProfiilpicPlaceholder.png
