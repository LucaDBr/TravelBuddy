platform :ios, '13.0'

# Explizit gültigen Pfad setzen
flutter_root = '/Users/builder/programs/flutter'
load File.join(flutter_root, 'packages', 'flutter_tools', 'bin', 'podhelpers.rb')

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  install_all_flutter_pods(File.dirname(File.realpath(__FILE__)))
end
