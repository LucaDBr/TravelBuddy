platform :ios, '13.0'

ENV['COCOAPODS_DISABLE_STATS'] = 'true'

# Wichtig: KEINE flutter_export_environment.rb einbinden!
require_relative '../Flutter/ephemeral/FlutterPods.rb'

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods(File.dirname(File.realpath(__FILE__)))
end
