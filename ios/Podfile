platform :ios, '13.0'

ENV['COCOAPODS_DISABLE_STATS'] = 'true'

use_frameworks!
use_modular_headers!

require_relative '../Flutter/ephemeral/FlutterPods.rb'

target 'Runner' do
  install_all_flutter_pods File.dirname(File.realpath(__FILE__))
end
